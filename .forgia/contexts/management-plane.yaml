# Bounded Context: Management Plane
# Generated by /arch-init from docs/ARCHITECTURE.md. Review and refine.

name: "management-plane"
responsibilities:
  - "Reconcile Vault CRs and deploy OpenBao StatefulSet accordingly"
  - "Manage OpenBao lifecycle: init, unseal, config, upgrade"
  - "Provide mutating admission webhook for pod secret injection"
  - "Apply declarative policy and auth method configuration"

ubiquitous_language:
  - term: "reconciliation"
    meaning: "Controller loop that compares Vault CR spec with live OpenBao state and makes them match."
  - term: "webhook injection"
    meaning: "Replacing vault-annotated env vars in pod spec with real secret values at admission time."

interfaces:
  - with: "data-plane"
    protocol: "http"
    contract: "OpenBao HTTP API on port 8200 — auth, secret read, policy write"
    direction: "outbound"
  - with: "application-layer"
    protocol: "http"
    contract: "Kubernetes admission webhook contract (AdmissionReview)"
    direction: "inbound"

dependencies:
  depends_on: ["data-plane"]
  depended_by: ["application-layer"]

status:
  reviewed: false
  fd_created: false
  fd_completed: false
