# Bounded Context: Data Plane
# Generated by /arch-init from docs/ARCHITECTURE.md. Review and refine.

name: "data-plane"
responsibilities:
  - "Store encrypted secrets with Raft HA backend"
  - "Serve OpenBao HTTP API for auth, read, write, policy operations"
  - "Enforce policies and ACLs at every request"
  - "Expose Prometheus metrics for observability"

ubiquitous_language:
  - term: "seal"
    meaning: "State where OpenBao is running but cannot decrypt data without the master key."
  - term: "leader"
    meaning: "The OpenBao replica elected by Raft that accepts writes. Followers replicate from the leader."

interfaces:
  - with: "management-plane"
    protocol: "http"
    contract: "OpenBao HTTP API — auth, secrets, policy, sys endpoints"
    direction: "inbound"

dependencies:
  depends_on: []
  depended_by: ["management-plane", "application-layer"]

status:
  reviewed: false
  fd_created: false
  fd_completed: false
