# C4 Level 2: Container Diagram
# Generated by /arch-init from docs/ARCHITECTURE.md. Review and refine.

containers:
  - name: "bank-vaults-operator"
    technology: "Go (controller-runtime)"
    description: "Reconciles Vault CRs, deploys OpenBao StatefulSet, manages unseal lifecycle."
    bounded_context: "management-plane"
    ports: ["8080"]
    depends_on: []

  - name: "vault-secrets-webhook"
    technology: "Go (controller-runtime)"
    description: "Mutating admission webhook that intercepts pod creation and injects OpenBao secrets into env vars, config maps, and custom resources."
    bounded_context: "management-plane"
    ports: ["8443"]
    depends_on: ["openbao"]

  - name: "openbao"
    technology: "Go (OpenBao server, MPL 2.0 Vault fork)"
    description: "Secret storage server with Raft HA backend. Handles encryption, policy enforcement, Kubernetes auth, and secret retrieval."
    bounded_context: "data-plane"
    ports: ["8200", "8201"]
    depends_on: []

  - name: "vault-cr"
    technology: "Kubernetes CRD"
    description: "Declarative specification of an OpenBao instance consumed by the Bank-Vaults Operator. Defines replicas, storage, policies, auth methods."
    bounded_context: "management-plane"
    ports: []
    depends_on: []

  - name: "unseal-secret"
    technology: "Kubernetes Secret"
    description: "Stores encrypted unseal keys for auto-unseal. In production replaced by cloud KMS or HSM."
    bounded_context: "data-plane"
    ports: []
    depends_on: []

  - name: "sample-app"
    technology: "Any (language-agnostic)"
    description: "Test workload that demonstrates transparent secret injection via vault annotation."
    bounded_context: "application-layer"
    ports: []
    depends_on: ["vault-secrets-webhook", "openbao"]
